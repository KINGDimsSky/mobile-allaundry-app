import { AppText } from "@/components/ui/app-text";
import { SafeAreaView } from "react-native-safe-area-context";

export default function SignInPage () {
    return (
        <SafeAreaView>
            <AppText>
                Halo ini untuk register
            </AppText>
        </SafeAreaView>
    )
}